# StreamForge development and runtime dependencies
# This file is self-contained: it includes the production dependencies plus
# the packages used by local tests, diagnostics, and development workflows.

# Core validation and settings
pydantic>=2.5.0
pydantic-settings>=2.0.0
typing-extensions>=4.8.0
python-dotenv>=1.0.0

# Kafka client
confluent-kafka>=2.4.0

# Prometheus metrics
prometheus-client>=0.19.0

# FastAPI control plane
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# CLI
typer>=0.12.0

# Optional production-only RocksDB backend. Install on Linux/Docker workers.
# rocksdict>=0.3.28

# Dev / test extras
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.27.0
psutil>=5.9.0
