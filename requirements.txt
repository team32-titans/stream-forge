# ==============================================================================
# StreamForge - Production Python Dependencies
# ==============================================================================
# Core typing and data validation (Pydantic v2)
pydantic>=2.5.0
typing-extensions>=4.8.0

# Enterprise Prometheus metrics exporter
prometheus-client>=0.19.0

# Kafka client (required by streamforge.workers.consumer / producers / infra)
confluent-kafka>=2.4.0

# Centralized settings (required by streamforge.config)
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Testing & chaos engineering
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Embedded LSM-Tree RocksDB backend (required only for STORAGE_MODE=production on Linux/Docker)
# rocksdict>=0.3.28
